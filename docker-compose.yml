services:
  cemantix:
    build: .
    container_name: cemantix-bot
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
    environment:
      - TZ=Europe/Paris
    command: ["python", "cemantix_bot.py"]
