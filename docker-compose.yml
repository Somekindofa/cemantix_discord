services:
  cemantix:
    build: .
    container_name: cemantix-bot
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      - ./data:/app/data
    environment:
      - TZ=Europe/Paris
    command: ["python", "cemantix_bot.py"]